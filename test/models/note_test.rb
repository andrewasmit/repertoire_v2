require "test_helper"
require 'faker'

class NoteTest < ActiveSupport::TestCase

  # test "Add Notes w/ correct validations" do
    # Variable Definitions
    # piece = Piece.create(
    #   composer: Faker::Name.name, 
    #   title: Faker::Quote.yoda,
    #   difficulty: Faker::Number.between(from: 1, to: 5),
    #   number_of_players: Faker::Number.between(from: 1, to: 5),
    #   genre: Faker::Music.genre
    # )
  
    # organization = Organization.create(name: 'Testing Organization')
    # password = Faker::Lorem.characters(number: 10, min_alpha: 4, min_numeric: 1)
    # username = Faker::Lorem.characters(number: 8, min_alpha: 1, min_numeric: 1)
  
    # user = User.create(
    #   username: username,
    #   password: password,
    #   password_confirmation: password,
    #   email_address: username + '@gmail.com',
    #   organization_id: organization.id
    # )


    # Test case definitions
    # note = Note.new
    # note.note = Faker::Quote.yoda
    # note.user_id = user.id
    # note.piece_id = piece.id

    # assert note.save,  "Note should save with correct validations"
  # end
end
